import 'package:flutter/material.dart';

class TerminalAmountScreen extends StatelessWidget {
  const TerminalAmountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Amount screen, yaaay!'),
      ),
    );
  }
}
